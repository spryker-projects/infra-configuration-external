locals {
  vpc_cidr_block  = "{{ vpc_cidr_block | default('10.105.0.0/16', true) }}"
  vpc_enable_ipv6 = {{ vpc_enable_ipv6 | default(false, true) | lower }}
  secondary_cidr  = {{ secondary_cidr | default(false, true) | lower }}
  public_nets = {{ public_nets | default('["10.105.1.0/24", "10.105.2.0/24"]', true) }}
  private_cmz_subnets = [
    "10.105.4.0/24",
    "10.105.5.0/24"
  ]
  private_middle_subnets = [
    "10.105.6.0/24",
    "10.105.7.0/24"
  ]
  private_dmz_subnets = [
    "10.105.8.0/24",
    "10.105.9.0/24"
  ]
  enable_vpc_peering_to_vpn = {{ enable_vpc_peering_to_vpn | default(false, true) | lower }}
  nat_redundancy            = {{ nat_redundancy | default(false, true) | lower }}
  enable_vpc_endpoint_apigw = {{ enable_vpc_endpoint_apigw | default(false, true) | lower }}
  enable_vpc_endpoint_s3    = {{ enable_vpc_endpoint_s3 | default(false, true) | lower }}
}
